# 引き継ぎメモ（新規セッション向け）

_最終更新: 2026-07-08（全13シェル完了 ＋ 1a〜1l をゲームとして遊べる化。残り 1m のみ）_

## このリポジトリは何か

Claude Design（claude.ai/design）からエクスポートされた TRPG UI デザインのハンドオフバンドル。
ルート直下に 13 種類の TRPG UI プロトタイプ（`1a`〜`1m` の `.dc.html`）が入っている。
それぞれ「全8画面＋デバイス3切替＋プレイスタイル3切替」の共通構造を持つ、世界観の異なる UI の「器」。

- `support.js` は Claude Design 自身のプレビュー用ランタイム（React ベースの `x-dc` テンプレートエンジン）。**実装には使わない**。

## 確定済みの実装方針（ユーザーに確認済み）

1. **技術スタック**: プレーン HTML/CSS/JS（ビルド不要、1ファイル完結、ダブルクリックで開ける）
2. **インタラクティブ性**: フルシェル実装
   - 8画面のナビゲーション切替
   - デバイス3切替（スマホ 392×828 / タブレット 900×660 / PC 1240×772）
   - プレイスタイル3切替（ソロ選択時は GM 画面をナビから消し、GM 画面表示中なら top にフォールバック）
3. 見た目はプロトタイプと**ピクセル一致**が目標

## ✅ 完了済み（2026-07-04）: 全13作品 + 一覧ページ

`implementation/` に 13 作品すべての単体動作 HTML と `index.html`（一覧ページ）がある。
各ファイルは自己完結（外部依存は Google Fonts のみ）で、`file://` のダブルクリックでも動く。

### 実装方式（旧方針からの変更点 — 重要）

当初は「1件ずつ手作業でテンプレートを JS 文字列に変換」する方針だったが、
13ファイルの構文が完全に統一されている（`sc-if` / `sc-for` / `{{ 単純変数参照 }}` / `onClick` のみ、
ロジックは `this.state` / `this.setState` のみ）ことを確認したうえで、
**共通ミニランタイムによる一括生成方式**に切り替えた（手転記ミスの排除と原本忠実性のため）:

- `tools/runtime.js` … `x-dc` テンプレートを解釈する約90行のランタイム（`DCLogic` 互換、React 不使用）。
  SVG 名前空間対応（1j に心電図 SVG あり）。状態変更ごとに全再構築（プロトタイプの再レンダーと同等）。
- `tools/build.js` … 各 `.dc.html` から `<helmet>`・テンプレート・ロジックを**原文のまま**抽出し、
  ランタイムを埋め込んだ自己完結 HTML を `implementation/` に一括生成。一覧 `index.html` も生成。
- 再生成: `node tools/build.js`（デザイン原本を直したらこれを再実行するだけ）

**注意**: 生成物の `<template>` 内には `sc-if` / `{{ }}` が意図的に残る（ランタイムが解釈する）。
旧メモにあった「sc-if が残っていないか grep 確認」は本方式では不要・非該当。
`window.__dcApp` にコンポーネントを公開している（動作検証・今後の拡張用）。

### 検証済み（実ブラウザで全13作品）

- 8画面すべてクリック遷移（1a・1b のみ `data-screen-label` 属性がソースに無いため見出しテキストで確認）
- プレイスタイル3切替: ソロで GM タブ消失（8→7）、GM 画面表示中にソロへ切替→ top へフォールバック
- デバイス3切替: フレーム寸法変更、スマホでレール→タブバー切替
- JS エラーなし

## 公開（2026-07-05〜）

- **公開URL**: https://andorole.github.io/trpg/ （GitHub Pages。スマホからも遊べる）
- リポジトリ: https://github.com/andorole/trpg （公開・アカウント andorole・gh CLI 認証済み）
- ルートの `index.html` は `implementation/` への転送ページ
- **更新手順**: ファイルを直したら `node tools/build.js` → `git add -A && git commit && git push`。
  push から1分ほどで自動的に公開へ反映される（Pages は main ブランチのルートを配信）

## この環境（Windows / OneDrive）での検証手順

旧メモの Playwright 手順は前の一時コンテナ（Linux）のもので、この環境では使わない。

- 簡易サーバ: `node tools/serve.js 8123`（日本語ファイル名対応）。`.claude/launch.json` に `static` 構成あり
- ブラウザで `http://localhost:8123/implementation/` を開いて一覧から確認
- もしくは `implementation/*.html` をダブルクリック（自己完結なので直接開ける）

## ✅ ゲーム層（2026-07-08）: 1a〜1l が実際に遊べる

### 1l-辺境コンビニ で加わった型

- レジ判定（d20）を**3段階＋出目1の特殊失敗**にした（成功／一部成功／失敗、出目1固定で
  「レジ詰まり」という専用の大失敗ラベル）。1g/1jの多段階判定に続く型だが、大失敗が
  「total の低さ」ではなく「出目そのもの」で決まる点が1jの判定Eと同系統
- combat画面（開門ラッシュ）が**共有の行列ゲージ＋4件の個別客対応**という構成。ただし
  1k（4本全部操作可）とは異なり、**常連客2件はあえて静的表示のまま**にして、新人パーティと
  ドラゴンの2件だけ増減ボタンを持たせた——「触れる必要がある要素だけ動的化する」という
  本プロジェクトの一貫方針を、1トラッカー内でも部分適用した初めての例
- 発注端末（inventory）の「地図の切れ端」査定を**story側の選択肢そのものの出現条件**に
  した（`items.mapStatus==='done'` を選択肢の `if` に直接使用）。他ゲームのアイテム連動は
  「読む/使うと専用の分岐が足される」型が多かったが、今回は逆に**査定前は選択肢②が
  存在しない**（隠しておいて、行動して初めて選べる）という一歩踏み込んだ連動
- シナリオ「最後の肉まんの、渡し先」約19ノード・エンディング8種（験担ぎの最大手/境界を
  守る側で/境界の釜を継ぐ/正直な報告/藪蛇の報告/いつも通りの朝、＋強制END2種）。
  GM秘匿2件（聖騎士ノエル＝大穴を守るため脱走した元騎士／おでん鍋＝境界の釜）を、
  地図の査定→ノエルへの相談→討伐隊の噂、という3段階の条件付き分岐で発見する真ルートあり
- 検証済み: レジ判定4段階（成功/一部成功/失敗/レジ詰まり）・sheet3ゲージ増減・
  行列ゲージ＋客トラッカー（新人/ドラゴンのみ増減、常連2件は静的）・地図査定→選択肢②
  の解禁・全主要分岐（①成功/①失敗/②知悉ルート/③即断エンド）・経営判定の成功/失敗
  分岐・強制END2種（体力→バックヤードで力尽きる／信頼→本部に見放される、いずれも
  「非終端ノードへの遷移時のみ発火・終端ノードへの遷移では上書きしない」の原則を
  維持して確認）・GM画面（幕切替・異変度±・来店ハプニング表d6）・セーブ復元・
  1a〜1kの回帰なし（12作品全部の全ナビボタンをiframeで一括クリックして確認）

### 1k-魔法少女コールセンター で加わった型

- combat画面が単一ボスでも並行未練でもなく、**4本の回線を独立に進行させるトラッカー**
  （敵幹部だけ「敵意が下がる＝良い」で他3本と方向が逆。`goodDir`で符号を反転させて
  ボタンの「進める／戻す」の意味を統一した）
- 一度きりの特殊行動（魔力残量3%を使う）をstory分岐の選択肢自体に組み込んだ
  （1aの「二度見」と同系統だが、今回は消費すると`maryoku`が0になり以後使えなくなる
  実際の資源消費を伴う点が新しい）
- 開発中に見つけたバグ: combat画面の共有ゲージ（街のパニック度）を他の値から算出する式にしたところ
  初期表示が原本の静的値42と食い違った。**派生値にせず、素直に独立したstateフィールドとして
  持たせる**のが正解だった（1d/1iの`lastRoll`不整合と同系統の「初期値と実際のロジックの不一致」）
- シナリオ「必殺技の名前が、決められない」約18ノード・エンディング7種。GM秘匿
  （敵幹部の正体＝初代魔法少女）は参照のみ、本筋はオペレーターすず自身の過去
  （引退の夜に叫んだ技名「また明日」）をノヴァの命名に重ねる構成
- 検証済み: CSAT5段階（大変満足〜切電）・4回線トラッカー・マグカップ連動・全主要分岐・
  強制END2種（声量→声が出なくなる／共感ゲージ→感情を閉ざす）・セーブ復元・1a〜1jの回帰なし。
  スクリーンショットツールがまた一時的にタイムアウトしたため、`preview_inspect`で代替確認した

### 1j-転生前人間ドック で加わった型

- 判定を**5段階の医療判定（A〜E）**にした。A=達成(dc以上)、B/C/Dは段階的に未達、
  出目1は特殊な「判定E＝魂が検査室から逃走」。1g/1iの多段階判定の3例目
- sheet画面が体力ゲージでなく**健診の数値表**（徳値・業値・未練残量・記憶清算率）そのもので、
  値を増減すると基準値との乖離度からA〜Dバッジが自動計算される（`gradeFromDev`ヘルパー）。
  ゲージバー方式でなく検査値方式にした初めての例
- combat画面が単一ボスでなく**未練3件を並行して「畳む／戻す」トラッカー**（1jで最大の未練が
  main objective、残り2件は副次目標という多重カウントダウン構成）
- バグ2件を検出・修正: ①初期`lastRoll`の等級が原本の例（基準15・出目17→判定A）と食い違う
  閾値ミス、②パッチ側で参照した`{{ nextOp }}`ハンドラをgame.js側に定義し忘れ（「次の処置へ」
  ボタンを押すとエラー）。**新しいゲームを書いたら、パッチが参照する全ハンドラ名をgame.jsの
  out.に実装したか、ボタンを実際に押して確認すること**（今回はビルド時のエラーでなく
  クリック時の実行時エラーだったため、全ボタンを一度は押すテストが必要だった）
- シナリオ「問診票の、最後の質問」約18ノード・エンディング8種。50年分の父子の後悔を扱う。
  GM秘匿（凪マチの生死）は今回サブ情報として参照のみ、本筋の分岐には使っていない
- 検証済み: 5段階判定・sheetの自動グレード計算・未練トラッカー・景色審査の連動・
  全主要分岐・強制END2種（徳拍→気絶／記憶清算率100%→強制成仏）・セーブ復元・1a〜1iの回帰なし

### 1i-洗濯龍 で加わった型

- 判定ダイスに初めて**2d10**を採用（他は全部d20判定）。専用の洗濯表示（〇成功／△部分成功／✕失敗）
  で結果を示し、ぞろ目（2つの出目が同じ）で特別ボーナス演出。dice screenの`diceSides`に
  文字列`'2d10'`を混在させ、`isCheck`判定を数値20でなく文字列一致に変更した初めての例
- combat画面が「工程バー」（予洗い→本洗い→すすぎ→脱水→乾燥の5段階、次の工程へで進む）＋
  ボスの汚れ残りゲージ（1d/1g型の0で勝利）＋味方3人（龍・潤・柚木さん）という4層構成
- 龍の「機嫌」が30未満で回転拒否タグが自動表示。洗剤棚の「龍のおやつ」→combatの「機嫌」という
  1h方式のアイテム→ミニゲーム連動をこちらでも採用
- バグ修正: 初期`lastRoll`オブジェクトに`mod`フィールドが無く、初期表示の判定式に「undefined」が
  混入していたのを検出・修正。1dでも類似の原因（初期`lastRoll`が`rollNow()`実行後の形と
  フィールド不一致）でバグを出しており、2件目。新しいゲームを書くたびに
  「初期lastRollは実際にrollNow()が生成するオブジェクトと全フィールドが一致しているか」を確認すること
- シナリオ「すすぎの水は、少し塩辛い」約18ノード・エンディング7種。GM秘匿
  （ドレスの染みの声＝6号機の中の声と同一人物）を発見する真ルートあり
- 検証済み: 2d10判定・ぞろ目ボーナス・工程バー進行・呪い撃破・機嫌低下と回転拒否・おやつ連動・
  全主要分岐・強制END2種（体力→気絶／清潔感→心が汚れきる）・セーブ復元・1a〜1hの回帰なし

### 1h-宛先不明郵便局 で加わった型

- combat画面が「戦闘」ではなく**旅路の3つの障り**（迷い霧＝晴れるまでのカウントダウン、
  未練の犬＝なつき度が100で採用、時空の踏切＝カウントダウンで「昨日/明日」が周期的に切替）＋
  **共有の時計**（夜明けまでの残り時間、mm:ss表示）という初めての構成。「先へ進む」1手が
  複数の独立したカウンター（霧・踏切・時計）を同時に動かす設計
- 夜明けの時計が0に達した瞬間、一度だけ自動でペナルティ（現世との縁−10）が発動する
  仕掛け（dawnPenaltyAppliedフラグで二重発火を防止）。1e の実在感自動減少と同系統だが
  「毎回」でなく「一度きりの締切超過」を表現する新パターン
- 郵袋の「犬用ビスケット」→combatの「なつき度」という、アイテム操作がcombat画面の
  数値を直接動かす初めての連動（他ゲームのアイテム連動はstory分岐が対象だったのに対し、
  今回はミニゲーム性のあるトラッカーを対象にした）
- シナリオ「渡し場、最終便」約20ノード・エンディング7種。GM秘匿（XX-0000-00の差出人＝
  帰り道を忘れた先代配達員）を発見する真ルートあり
- 検証済み: 消印3段階・霧/犬/踏切/時計の同時進行・締切超過ペナルティの一度きり発火・
  ビスケット連動・全主要分岐・強制END2種（体力→気絶／現世との縁→帰ってこない）・
  セーブ復元・1a〜1gの回帰なし

### 1g-反省文ドラゴン で加わった型

- d20判定を**3段階**で判定（承認／条件付差戻／差戻）にした。「条件付差戻」は失敗ではなく
  「難度未達だが僅差」を表す独立の帯（dc-3〜dc未満）。1c の星評価に続く、判定結果の多段階化の2例目
- ステータス値を**数値でなくラベルで表示**する型（上司からの信頼: 低下中／回復中／良好、閾値で切替）。
  内部は0-100の数値のまま保持し、表示だけラベル化（1b の六道ダイスと同系統の「意味づけレイヤー」）
- ボスゲージ（呆れ）を0まで下げれば勝利という1dと同型のカウントダウンだが、GM用の手動+/-ボタンを
  combat画面に常設し、パーティ側の判定成功が呆れを下げる、という因果を暗黙のうちにプレイヤー操作へ委ねる設計
  （自動連動はさせず、既存パターン通り手動調整に留めた——過剰な自動化を避ける判断）
- シナリオ「会議室Cが全焼するまでの経緯」約20ノード・エンディング7種。添付資料の「本音の下書き」に
  印をつけると終盤の選択肢が変わる連動（1a巻物・1e手紙・1f日記に続く4例目）。GM秘匿
  （上司ユーリ＝竜たちのリストラを阻止しようとしている）を発見する真ルートあり
- 検証済み: 決裁印3段階・全主要分岐・強制END2種（体力→気絶／社会性→本性化）・セーブ復元・
  1a〜1fの回帰なし。スクリーンショットツールが一時的にタイムアウトしたため、最終見た目確認は
  `preview_inspect`（文言・色・寸法の直接検証）で代替した

### 1f-左心室不動産 で加わった型

- 判定結果を「家賃を実際に下げる」効果に直結（値下げ交渉の成功で `rent` state を減算し表示に反映）。
  選ぶ技能によって判定の意味が変わる設計（値下げ交渉以外は一般的な審査通過／却下のみ）
- 免疫対応（combat）は倒す/守るのどちらでもなく、**パーティ共有ゲージが満タンになると
  巻き戻りイベントが起きる**方式（1d のカウントダウン式・1c の御満悦度式に続く3つ目のゲージ型）。
  入居者ごとに「異物認定（毎ターン+6）／消臭済み（+0）」をトグルでき、手番送りのたびに
  識別中の人数分ゲージが伸びる。満タンで「くしゃみ」発生 → ゲージ20へ巻き戻り＋全員に軽い被弾
- 押収品/契約書類系のアイテム連動（1a の巻物・1e の手紙と同型）を「前の住人の日記」に適用。
  読むと GM 秘匿（営業担当カナメ＝前の住人）へ到達する専用選択肢が解禁される
- シナリオ「内見、はじまる（ドクン）」約18ノード・エンディング7種
- 検証済み: 値下げ交渉の家賃反映・心室痙攣（大失敗）・トグル操作・日記連動・全主要分岐・
  強制END2種（体力→気絶／宿主適合率→異物認定で退去）・セーブ復元・1a〜1eの回帰なし

### build.js のバグ修正（重要・全ゲームに影響する共通基盤）

1e で `block:` パッチが「ブロック内に入れ子の sc-if があると失敗する」ことが発覚。
原因は `</sc-if>` を `indexOf` で単純検索しており、ネストがあると**内側の閉じタグを外側の閉じタグと誤認**していたこと
（1a〜1d はたまたま置換対象ブロックにネストが無かったため症状が出なかった）。
`tools/build.js` の `findMatchingClose()` で深さを数える正しい対応付けに修正済み。
**次のゲーム化以降、既存4作品の回帰確認込みで検証してから進めること**（今回は実施し、1a〜1d 全画面に問題なし）。

### 1e-存在しない国の入国審査 で加わった型

- **原本にすでにあった手作りの相互作用（`rechecked` トグル）とゲーム層の共存**。
  `GameEngine.attach` は `renderVals` を「土台の結果に上書きマージ」するだけなので、
  ゲーム層側で同名キーを定義しなければ原本のロジック（`doc`/`recheck`/`recheckLabel` 等）はそのまま動き続ける
- 判定を下すたび**手帳のステータス（実在感）が自動で減る**仕掛け（rollNow 内で jitsu-1 を同時に setState）。
  テーマ（審査を続けると自分の実在が薄れる）をメカニクスに直結させた
- 検問の列は「敵を倒す」ではなく**旅行者ごとに矛盾の発見数を数えるトラッカー**（3/3 で「審問へ移行可能」表示）。
  正体不明の旅行者は追跡不可（？/？固定、ボタンなし）、「6+」の行は数えるたびに人数が増えるお遊び要素のみ
  （`fogCount` を押すたびインクリメントするだけの非マンダトリー機能）
- 押収品保管庫は**1点だけ操作可能**（宛先が自分自身の手紙を開封）にして、他の押収品は原本のまま静的に残した。
  開封すると審査記録の最終選択肢に専用の分岐（メタ的なエンディング）が追加される、という画面をまたいだ連動
- シナリオ「「アンナ・ヴェイル」の審査」約20ノード・エンディング9種。特性「二度見」を一度きり使える選択肢として実装。
  GM秘匿（アルカディアは彼女が眠るたびに作り直される国）を発見する真ルートと、手紙を絡めたメタエンドあり
- 検証済み: 審査印3パターン・判定ごとの実在感自動減少・既存recheckトグルとの共存・列トラッカー操作・
  手紙開封の連動・全主要分岐・強制END2種（体力→居眠り／実在感→老人化）・セーブ復元・1a〜1dの回帰なし

### 1d-確定申告ダンジョン で加わった型

- ボスの HP を「倒す」でなく「0にすれば勝ち」の**カウントダウン式ゲージ**（追徴予定額 300,000G→0）。
  増減ボタンの刻みをキャラごとに変える（ボスは±1万/±3万、他は±1/±5）
- 判定結果を**印影（承認／差戻／差戻・重加算税）**で表現。出目20は「判例となる」特別演出。
  還付額を判定の余剰値から算出し自動で所持金に加算（rollNow 内で patch.decGold を更新）
- 領収書を個別カードとして「計上する／開ける／廃棄する」ボタンで操作 → 申告済/未計上額が実際に動く
- バグ修正: 初期 lastRoll に `ok` フィールドが無く判定表示が壊れていたのを、outcome から ok を
  導出するフォールバックで解消（stampInfo 内）。他ゲームへの横展開時は要注意
- シナリオ「窓口にて、金貨三百枚の件」約24ノード・エンディング8種。GM秘匿
  （査察官マルサの愛刀「更正」が20年間経費で落ちていない）を交渉材料として発見する真ルートあり
- 検証済み: 査定印3パターン・ボス撃破・領収書操作・全主要分岐・強制END2種・セーブ復元・エラーなし

### 1c-深宴 で加わった型

- 判定結果の段階評価: d20 判定を成功/失敗の2値でなく **星評価**（基準+8=三つ星 / +4=二つ星 / 達成=一つ星 /
  未達=星なし / 出目1=星なし・粗相）にした。starRank() が {表示, 色, ok} を返す
- 敵ではない相手: 賓客「深き沖の伯爵」は倒す対象でなく **御満悦度ゲージ**（tokenStyle を丸ごと
  パラメータ化して発光トークンに）。0 になると「御立腹」
- 鑑定の口上は漢数字（kanjiNum は 0〜199 対応）
- シナリオ「御客人、匙を置く」約22ノード・エンディング9種（真結: お帰りの一皿。名乗って契約する
  変化球エンド E_follow あり）。氷室の"触れるな"食材と客人台帳の伏線を回収
- 検証済み: 星評価各段階・全分岐・強制END 2種（気力→失神 / 常心→悲鳴）・セーブ復元・エラーなし

### 1b で加わった型（世界観固有メカニクスの例）

- ゲージの名前・意味は作品ごとに変える: 1b は 気力(14) / 霊力(10) / **正気(0–100, CoC風)**。
  正気枯渇で強制エンディング「水鏡」、気力枯渇で「泥の味」（choose() 内で二段チェック）
- 敵だけ別ゲージ: 影喰いは「瘴気」ラベル＋黒バー（combat 行に gauge / barBg をパラメータ化）
- 特殊ダイス: 「六道」= d6 で地獄道〜天道（diceSides に数値以外の値を許す）
- 和風の数量表記: 数量・銭を漢数字表示（kanjiNum / kanjiMoney。内部は数値のまま）
- シナリオ「沼の底の、もうひとり」約20ノード・エンディング7種（影還し/憶えている/祓い済み/
  割れる音/夜明け/泥の味/水鏡）。GM 秘匿（影喰い＝水死した娘みお・鏡の理）を真相として回収
- 検証済み: 全画面機能・全分岐（乱数固定）・強制END 2種・セーブ復元・コンソールエラーなし

シェルの上に「ゲーム層」を重ねる 2 層構成。**残り12作品もこの型で量産する**（ユーザー確認済み）。

### アーキテクチャ

- `tools/engine.js` … 全作品共通。ダイス / d20判定（20=大成功・1=大失敗）/ localStorage 自動セーブ /
  `GameEngine.attach(app, game)` で `renderVals` にゲーム値を合流・`setState` に永続化を注入。
  `app._resetGame()` でゲーム状態のみ初期化（シェル状態は保持）
- `games/<id>.patches.js` … **ビルド時**にテンプレートの静的ブロックを動的テンプレートへ差し替えるパッチ。
  `{ find, replace }`（完全一致・1回のみ）と `{ block: 'isDice', replace }`（sc-if ブロック丸ごと）の2種。
  見た目のスタイル文字列は原本からそのまま転記する（＝デザイン維持の要）。
  ※ block 置換は「元ブロックに入れ子 sc-if が無い」ことが前提（build.js が検査して違反なら失敗する）
- `games/<id>.game.js` … **実行時**。`window.__GAME__ = { id, initialState, persist, migrate, vals }` を定義。
  `vals(app)` が画面用の値とクリック用クロージャ（シェルの `go` と同じ流儀）を返す
- build.js が `games/1x.patches.js` / `games/1x.game.js` の存在を見て自動で取り込む。一覧ページに「遊べる」バッジ

### 1a で実装した遊び（次作品の雛形にする）

1. **判定**: d4〜d100 選択・技能ピル（隠密+7 等）・難度±・実ロール・「巻末の記録」ログ（全画面共有・上限10件）
2. **人物録**: HP/MP 増減（戦況・物語・霊薬と同一の状態を共有）
3. **戦況**: イニシアチブ順トラッカー。手番送り（戦闘不能スキップ・一巡でラウンド+1）・±HP・HP0で「倒れた」
4. **持ち物**: 所持金・数量が実データ。癒しの霊薬「使う」で HP+7。シナリオ報酬（王家の冠等）もここに入る
5. **物語（ソロプレイの本体）**: 分岐シナリオ「紫の狼煙」約25ノード。判定つき選択肢（成功/失敗分岐・
   判定結果は本文上部に表示＆ログにも記録）・フラグ条件選択肢・HP消耗・アイテム消費（巻物）・
   エンディング7種（真結/誓い/器/遠雷/撤退/灰の中へ/最古の記憶）・HP0で強制敗北END・読み直し
6. **進行(GM)**: 場面切替・緊張度±・乱表 d6 ロール
7. GMの秘匿情報（亡霊=かつての王）をシナリオの真ルートとして回収する構成にした

### 検証済み（実ブラウザ）: ロール/ログ/HP同期/霊薬/戦闘不能スキップ/全分岐（乱数固定で失敗・大成功・強制ENDも）/リロード後のセーブ復元/ソロ切替のシェル動作/コンソールエラーなし

## 残タスク・構想

1. **残り1作品のゲーム層（1m-末期ゴミ収集車）**（手順: ①原本 .dc.html を読む → ②games/1m.patches.js で
   dice/combat/story/GM ブロック等を動的化 → ③games/1m.game.js に世界観に合わせた判定・トラッカー・
   分岐シナリオを書く → ④node tools/build.js → ⑤ブラウザ検証。1a〜1lのどれかをコピーして改変が速い）
   - シナリオは GM 画面の「秘匿事項」を回収する筋にすると据わりが良い（1a・1d・1e・1g・1h・1i・
     1j・1k・1l 全部この型。1m も該当するはず）
   - 1件終わったら本メモの更新手順どおり: HANDOFF.md にセクション追記 → commit → push
2. デザイン原本の微調整があれば `.dc.html` を直して `node tools/build.js` で再生成
